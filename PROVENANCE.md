# Provenance — every reported number maps to a script

Convention borrowed from `reliafair`: no number is quoted anywhere in this
repository unless this file says which script produced it and where it lives.

| Artifact | Produced by | What it supports |
|---|---|---|
| `results/e1_coverage.csv` | `experiments/run_validation.py` (E1) | H2 smoke test: coverage = 1.000 at the true (Λ, π) in all 4 regimes × 500 reps; coverage collapses to 0.10–0.34 at 0.5× misspecification (bounds fail informatively); stays 1.000 at 1.5× (conservative is safe). Sharpness ratio ≈ 0.27 (true bias ≈ 27% of worst-case width under random, non-adversarial injection). |
| `results/e3_leaderboard_audit.csv` | `experiments/run_validation.py` (E3) | Blind flagging demo: the one claim involving the secretly contaminated model on the winning side ("m2_contam ≻ m3", Γ\* = 0.173) is the only non-robust adjacent claim at CSM(0.25, 0.2); clean claims have Γ\* ∈ [0.29, 0.63]. The audit does not know which model was contaminated. |
| `results/gate_analysis.csv`, `results/gate_verdict.txt` | `experiments/run_validation.py` (E4) | Phase-0 vacuousness gate: 1-pt frontier margins overturnable in 81% of plausible (Λ, π) cells; 10-pt cross-tier margins in 12%. Discriminating regime → **PASS** (THEORY.md §7). |
| `results/figures/f2_identified_set.png` | `experiments/run_validation.py` (E2) | The money figure: identified-set bands vs Λ for three π, with true θ and true Λ overlaid. |
| `results/figures/f5_frontier.png` | `experiments/run_validation.py` (E3) | Contamination frontier Γ\*(π): survives/overturnable regions for a leaderboard claim. |
| Unit-test claims in THEORY.md (sharpness attained, reductions, monotonicity, ceiling effect) | `tests/test_bounds.py`, `tests/test_inference_and_leaderboard.py` | 19 tests, all passing (`python -m pytest tests -q`). |
| v0.2 upgrade claims in THEORY.md §9 (two-sided reduction at ρ=0, inflation hand case, grouped ≤ item-level, singleton-group equality, two-sided frontier Δ/(π(1+ρ))) | `tests/test_v02_upgrades.py` | 8 tests; full suite 27/27 passing. |
| Novelty verdict, reviewer simulation, and score deltas | `RED_TEAM_REPORT.md` | Red-team cycle of 16 July 2026; 6 adversarial literature rounds; simulation disclaimer applies. |
| `results/p1_t2_consistency.csv`, fig f10 | `experiments/run_p1_theory.py` (T2) | Theorem 4 empirics: Γ̂* RMSE slope −0.518 (theory −0.5), bias ≤0.0025 for n ∈ [100, 25600]. |
| `results/p1_t3_finite_draws.csv`, fig f11 | `experiments/run_p1_theory.py` (T3/E9) | Finite-draw plug-in bias −40% (m=2, λ=0.1) → +4% (m=10) → <1% (m=50); joint-interval coverage ≥0.993; simple-bound coverage 1.000. Prescription m≥10. |
| fig f9 (bridge interpolation) | `experiments/run_p1_theory.py` (T1) | Prop 3: random (πd̄) vs adversarial endpoints differ ~1.5× at λ=0.3, π=0.15; `test_p1_theory.py` verifies both endpoints exactly. |
| `results/lambda_priors.csv` | manual literature harvest, 17 Jul 2026 (links in file) | Λ calibration priors: central range [0.1, 0.45], task-heterogeneous (MMLU ≈ 0.02 low; HellaSwag ≈ 0.4 high); strongest anchor LLaMA-70B HellaSwag clean 63.5 vs dirty 78.8 → Λ ≈ 0.42. |
| `PREREGISTRATION.md` | drafted before any confirmatory Γ\* computed | H4 (≥25% of claims non-robust), corpus, estimator, Λ protocol, exclusions. Pilot pairs flagged exploratory. |
| `results/p4_pilot_atlas.csv`, fig f13 | `experiments/run_p4_pilot_atlas.py` | **EXPLORATORY PILOT** on the author's TSFM atlas (17 shared datasets, 6 models, skill = 1/(1+MASE)): every adjacent ranking claim has Γ\* ≤ 0.114 at π=0.1 (≤ 0.034 at π=0.5) — at/below the calibrated Λ range. Stratification (14/17 datasets high-prior) barely helps: the eligible set is nearly everything. |
| `results/p5_design.csv`, fig f14 | `experiments/run_p5_design.py` | Design sensitivity: fresh-item fraction 0→0.9 lifts Γ\* 0.168→0.453 at a 3-pt margin (D1); sharp knapsack vs simple bound Γ\* 0.172 vs 0.154 at m=20 draws (D2). |
| `notebooks/contamctrl_lora_kaggle.ipynb` | `notebooks/make_notebook.py` (regenerate with `python notebooks/make_notebook.py`) | Full-scale CONTAM-CTRL scaffold (Qwen2.5-1.5B/TinyLlama, MMLU 4 subjects, π×dose grid). **Not yet executed** — requires Kaggle T4. v2 (17 Jul): fixed collapsed `\n` escapes (user-reported SyntaxError in cell 2), switched bfloat16→float16 (T4 has no proper bf16), device via `next(model.parameters()).device`; the generator now compile-checks every cell before writing. v3: uninstall torchao in setup — Kaggle ships 0.10.0 and recent peft raises `ImportError` during LoRA injection unless torchao is ≥0.16 or absent (user-reported). v4 (after user's full-grid run was interrupted with zero output saved): per-config CSV checkpointing + skip-completed resume, base model loaded once per model with `unload()` between configs (saves ~30–60 min), twin-integrity assertion after every unload (drift < 2e-3 on a 16-item probe), step-level progress prints every 50 steps so dose-16 configs never look hung, eval batch 8→16. v5 (user's fp16 run: Qwen 12/12 configs clean with a textbook dose-response — dose 1 ≈ 0 or negative, dose 16 ≈ +34–41pt — but ALL TinyLlama configs NaN'd from fp16 llama-family training overflow): base dtype → float32, TinyLlama → **Qwen2.5-0.5B** (same-family pair isolates capacity; ships safetensors, silencing the harmless auto-conversion threads), NaN guard refuses to checkpoint corrupted configs. |
| `results/p2_contamctrl.csv`, fig f12 (two-panel) | `experiments/run_p2_contamctrl.py`, `make_f12_two_panel.py` | THEORY §11: emergent-memorization channel test. env_corr ≥ +0.91 in 15/15 configs; Λ̂ by capacity (logreg 0.02–0.23 dose-responsive, MLP ≈0.96, RF =1.00); violation rate up to 27%; spillover 0.003–0.20. |
| `results/p2b_spillover_fix.csv` | `experiments/run_p2b_spillover_fix.py` | U4 remedy verification: coverage 1/3–2/3 → **3/3 in all six previously failing configs** with ε̂-widened intervals (ε̂ from calibration seeds 0–2, tested on seeds 3–5). |

| Forensic audit fixes (17 Jul 2026): binary-regime guard (`is_binary_like`, auto-routing, Γ\*=1.0 discontinuity documented), pilot eligible-mask conservatism, ρ across the API, `fragility_pvalue` + `bh_fdr` (the pre-registered FDR layer), duplicate-draw aggregation made explicit | `tests/test_audit_fixes.py` (13 tests) + THEORY.md §5 degeneracy note | Pilot re-run after mask fix: Γ\* deltas ≤ 0.002, conclusions unchanged, direction now conservative. E3 demo reproduces digit-for-digit under the corrected regime logic. |
| One-command reproduction | `reproduce.py` | Fast path: full test suite + P0/P1/P5/P4-pilot/P2c in ~52 s with `results/run_manifest.json` (platform, versions, steps); `--full` adds the Phase-2 grid. |
| Re-audit v2 (17 Jul 2026): Imbens–Manski parameter-level intervals (default `target="parameter"`, erf-based, no new deps), Benjamini–Yekutieli dependence-robust FDR option, CI-inversion documentation for fragility p-values, reference-free claim precision (compute vs interpret), calibration quality policy (low/corroboration/qualitative rows excluded), ranking-selection caveat, dead-code removal, run manifest | `tests/test_v2_reaudit.py` (7 tests incl. worst-case IM coverage sim ≥0.90 at nominal 0.95) + PREREGISTRATION §3–4 updates | Regression protocol caught and resolved one intentional behavior change (set-level upper-endpoint invariance vs IM adaptive tightening — both properties now pinned separately). |
| `results/contamctrl_lora_peritem.csv` (**complete 24/24 fp32 grid**; fp16 QUICK archived as `_fp16_quick.csv`; duplicate backup in `results/contamctrl_output/`) | `notebooks/contamctrl_lora_kaggle.ipynb` v5, executed by the author on Kaggle T4 across two sessions (18 Jul 2026): browser crash mid-run preserved 20/24 via checkpointing; the resumed session (checkpoint recovered from /kaggle/input) completed the 0.5B tail and **reproduced the prior 20 configs bit-exactly** | Raw LLM-scale CONTAM-CTRL: Qwen2.5-1.5B + 0.5B, 905 MMLU items, full π × dose grid. Integrity: zero NaNs, exact contaminated counts, y_clean cross-config spread exactly 0 across all 24 configs. |
| `results/p2c_lora_llm_scale.csv`, figs f15–f16, 6 aggregated 'measured' rows in `lambda_priors.csv` | `experiments/run_p2c_lora_analysis.py` | THEORY §11 dose law (final): dose 1 net-NEGATIVE in **7/8** configs (violations 31–56%), dose 16 = complete memorization (**Λ̂=1.000 in all 8**, lifts to +56pt, violations ≤0.9%); ρ dose-dependent; raw lift anti-orders with capacity while Λ restores ordering; spillover 0.04→0.30; cross-config consistency **21/24** — all 3 failures are pooled-λ̂-meets-dose-16-high-π, evidencing the mandated dose-stratified calibration. |
| `results/p6_oll_demo.csv` | `scripts/fetch_oll.py` | **EXPLORATORY DEMO** on real Open LLM Leaderboard v1 archive details (ARC-Challenge, 1,170 paired items, 4 models, binary → auto simple regime): Llama-3-8B ≻ Mistral-7B Γ\*=0.026 (fragile); Mistral ≻ Llama-2-13B Γ\*=0.188 (within calibrated Λ); Llama-2-13B ≻ Llama-2-7B Γ\*=0.504, fragility p=0.006, **FDR-certified robust**. Loader handles v1 format generations + tiny debug snapshots. |

| Pre-registration freeze | `PREREGISTRATION.md` v1.0 + git tag `prereg-freeze-v1.0` (commit ca7773b, 19 Jul 2026) + `OSF_FILING.md` | Frozen BEFORE any confirmatory Γ\*: corpus (16 models × 5 v1-archive tasks), primary rule Γ\*(π=0.1) < Λ_ref, Λ_ref = 0.355 pooled / 0.42 HellaSwag (medians of quality-eligible prior rows), BH primary + BY sensitivity, ρ ∈ {0, 0.2}. **Pre-registered on OSF 20 Jul 2026: <https://osf.io/2436z>, DOI 10.17605/OSF.IO/2436Z** (public, un-embargoed; Internet Archive mirror `osf-registrations-2436z-v1`). Attached to the registration: the protocol verbatim, `results/lambda_priors.csv` (blob `590d295…`), `THEORY.md`, and `OSF_ADDENDUM_chain_of_custody.md`. |
| `paper/appendix_proofs.md` | this repo | Formal proofs: Prop 1 sharpness, Cor 1–2, Lemma A.1 (R2 degeneracy + jump), Prop 2 two-sided, Prop 3 bridge (LP extreme-point argument), Thm 4 (L-statistic consistency + delta method + bootstrap), Lemmas A.2 (E9 bias directions), A.3 (IM critical value), A.8 (spillover). Each result names its pinning unit test. |
| Corpus-mix notebook mode | `notebooks/make_notebook.py` v6 (`RUN_MODE="corpus_mix"`) | Dilution experiment: leaked items interleaved with wikitext-103 chunks at mix ∈ {0, 4, 20} per leaked copy (1.5B, π=0.1, dose {1,4}); `mix` column added to the CSV schema; **not yet executed** (Kaggle T4, ~1–2 h). |

| `results/confirmatory_audit.csv`, `confirmatory_summary.txt`, fig f17 | `scripts/run_confirmatory_audit.py` (protocol v1.0 @ tag `prereg-freeze-v1.0`; executed 19 Jul 2026; pre-registered <https://osf.io/2436z>) | **H4 CONFIRMED: 49/58 = 84.5% of adjacent claims not contamination-robust** (exact two-sided CI [72.6%, 92.7%]; one-sided p = 2.7e−21 vs the 25% threshold; excl. pilot overlap 47/56 = 83.9%). ρ=0.2 unchanged. BH certificates 8/58, BY 6/58 — every certified claim has margin > 5 pt. Median Γ\*(0.1): ARC 0.120, HellaSwag 0.084, Winogrande 0.087, GSM8K 0.212. Protocol notes (logged): TruthfulQA dropped (mc1/mc2 ≠ frozen acc spec); falcon-7b dropped on ARC (item-universe mismatch, amendment 1); GPT-J dropped on Winogrande (malformed score column). |

Global seed: 42. All experiments CPU-only (Kaggle notebooks excepted); fast reproduction ~45 s, full ~30 min. Confirmatory audit (`scripts/run_confirmatory_audit.py`) is network-dependent and intentionally NOT part of `reproduce.py` — it re-executes the frozen protocol and should be run deliberately.
